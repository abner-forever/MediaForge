# Sentry DSN — 需要替换为你自己的 Sentry 项目 DSN
# 无 DSN 时 Sentry 不会初始化，不会影响开发
# VITE_SENTRY_DSN=https://your-dsn@o000000.ingest.us.sentry.io/0000000

# 可选：在开发环境也启用 Sentry（设置 =1）
# VITE_SENTRY_DEBUG=1

# 可选：指定上报的环境名称（默认 production/development 自动判断）
# 本地 desktop:start 会自动传 development，一般无需手动设置
# VITE_SENTRY_ENV=staging
